# Google AI Studio API Key for Gemini feedback generation
GEMINI_API_KEY=your_gemini_api_key_here

# Internal shared secret for verifying calls from the Node Express backend
AI_SERVICE_SECRET=a_strong_random_secret_for_internal_auth

# Allowed origin for CORS (e.g., backend URL)
ALLOWED_ORIGIN=http://localhost:5000
